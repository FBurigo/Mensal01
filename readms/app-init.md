# backend/app/__init__.py

**Localização:** `/backend/app/__init__.py`

## O que faz

Arquivo vazio que marca a pasta `app/` como um pacote Python. Sem ele, os imports como `from app.database import get_db` não funcionariam.

## Dependências

- Nenhuma — arquivo estrutural do Python.
